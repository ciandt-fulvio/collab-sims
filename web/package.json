{
  "name": "sims-web",
  "version": "1.0.0",
  "type": "module",
  "description": "Sims frontend tests",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "happy-dom": "^12.10.3",
    "vitest": "^1.0.0"
  }
}
